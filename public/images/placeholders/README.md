# Temporary stock images

## Stock photos (replace with your real photos)
See `stock/README.md` — files like `arul-headshot.svg` are labeled placeholders.
Replace with JPG/PNG/WebP and update paths in `lib/personal-data.ts` → `stockPhotos`.

## Venture / skill posters
SVG posters under `posters/` and heroes under `hero/` keep the Netflix UI complete.
Replace any file — keep the same filename — or update `lib/design-tokens.ts`.

## Videos
Drop MP4s into `public/videos/` (see `public/videos/README.md`).
Optional YouTube IDs live in `lib/personal-data.ts` → `mediaVideos`.
