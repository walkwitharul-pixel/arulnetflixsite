# Videos (replace these)

Drop your own MP4 files here using the exact filenames below. The site will auto-detect them.

| File | Used for |
|------|----------|
| `intro.mp4` | About / profile intro trailer |
| `philosophy.mp4` | Entrepreneurial philosophy |
| `velantec.mp4` | VELANTEC overview |
| `mrassistant.mp4` | MrAssistant.Ai demo |
| `growthlab.mp4` | GrowthLab community |
| `onestopsg.mp4` | ONESTOPSG marketing |

**Tips**
- Prefer H.264 MP4, 16:9, under ~50MB for fast loading.
- Optional: set a `youtubeId` in `lib/personal-data.ts` as a fallback until your file is ready.
- Thumbnails live under `public/images/stock/` and `public/images/placeholders/posters/`.

Until a file exists, the player shows a clear “Add your video here” state instead of breaking.
